name: Odoo 14

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  integration-test:
    uses: ./.github/workflows/integration-test.yml
    with:
      odoo-version: "14"
      docker-profile: "--profile odoo14"
      port: "8014"
